@{
    ViewData["Title"] = "Clients";
}

<div>
    <partial name="Partials/Sections/_AddClientForm" />
    <partial name="Partials/Sections/_EditClientForm" />
</div>

<section id="clients">
    <header class="clients-header">
        <h2>Clients</h2>
        <button type="button" data-modal="true" data-target="#addClientModal" class="btn btn-add btn-blue">
            <i class="fas fa-plus"></i>
            <span>Add Client</span>
        </button>
    </header>

    <div class="surface">
        <partial name="Partials/Sections/_ClientList" />
    </div>
</section>
